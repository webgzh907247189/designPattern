{
  "compilerOptions": {
    "outDir": "dist", // 输出的目录
    "sourceMap": true, // 采用 sourcemap
    "target": "es2016", // 目标语法
    "module": "esnext", // 模块格式
    "moduleResolution": "node", // 模块解析方式
    "strict": false,  // 严格模式
    "resolveJsonModule": true, // 解析 json 模块
    "esModuleInterop": true, // 允许通过 es6 语法引入 cjs 模块
    "jsx": "preserve", // jsx 不转译
    "lib": ["ESNext", "DOM"], // 支持的类库 

    // 服务于 monorepo 项目的 模块依赖查找
    "baseUrl": ".", // 引入模块 以当前模块为基准
    "paths": {
      "@vue/*": ["packages/*/src"] // 找 @vue/* 下面的包 去对应的下面找
    }
  }
}
