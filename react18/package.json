{
  "name": "react18",
  "version": "1.0.0",
  "description": "1. 帮助应用保持响应，根据用户的 设备性能 和 网速 进行调整 2. 在 concurrent 模式 渲染不是阻塞的，可以中断 3. 出现了 更新优先级(对比 git hotfix 的概念进行对比) 的概念 (react 18之前没有更新优先级的概念，所有的更新都是需要排队的)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite",
    "build": "tsc && vite build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/react": "^18.0.17",
    "@types/react-dom": "^18.0.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react-refresh": "^1.3.6",
    "typescript": "^4.8.2",
    "vite": "^3.0.9"
  }
}
